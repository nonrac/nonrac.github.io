function packCode() {
    var inputCode = document.getElementById('codeInput').value;
    // Send the inputCode to the backend for packing
    // Receive the packed code from the backend
    // For demonstration, I'll just alert the packed code
    alert("Packed code: " + inputCode);
}
